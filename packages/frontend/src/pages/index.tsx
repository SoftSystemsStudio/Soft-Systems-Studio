import React from 'react';
import Layout from '../components/Layout';

export default function Home() {
  return (
    <Layout>
      <main style={{ padding: 40 }}>
        <h1>Soft Systems Studio</h1>
        <p>
          Next.js frontend placeholder. Connect to the API at <code>/api</code>.
        </p>
      </main>
    </Layout>
  );
}
