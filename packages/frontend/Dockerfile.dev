FROM node:22
WORKDIR /workspace
RUN corepack enable && corepack prepare pnpm@8.11.0 --activate
EXPOSE 3000
CMD ["sh", "-c", "corepack enable && corepack prepare pnpm@8.11.0 --activate && pnpm --filter frontend dev --hostname 0.0.0.0"]
