# Root / shared env example
# Copy this file to `.env` for local development and fill in values.
# DO NOT commit real secrets to the repository.

# Databases
# Primary Postgres connection used by Prisma and the API.
POSTGRES_URL=postgresql://postgres:password@localhost:5432/softsystems
# (Optional) Some packages expect `DATABASE_URL` — keep in sync with POSTGRES_URL
DATABASE_URL=${POSTGRES_URL}

# Redis (used for BullMQ queue)
REDIS_URL=redis://localhost:6379

# Qdrant vector DB (HTTP endpoint)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=default

# OpenAI (or equivalent LLM provider)
OPENAI_API_KEY=your-openai-api-key

# Application / runtime
NODE_ENV=development
PORT=4000

# Auth
# Use a strong secret in production. If set, JWT auth will be enabled for the API.
JWT_SECRET=replace-with-secure-random-value
JWT_ALGORITHM=HS256

# Observability (optional)
# SENTRY_DSN=your-sentry-dsn

# Third-party service placeholders (fill as needed)
SENDGRID_API_KEY=your-sendgrid-api-key
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Notes:
# - For local development use docker-compose in `/infra` to run Postgres/Redis/Qdrant.
# - Keep secrets out of version control; use a secrets manager in production.
